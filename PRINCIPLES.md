# ColaMD Principles

产品与工程上的判断标准。规范的细节在各自的文件里，这里记录**为什么这样定**，以及踩过的坑。

## 适用范围

这份文档对**所有参与者**生效：维护者、外部贡献者，以及在本仓库里干活的 Agent。动手之前先读 [AGENT.md](AGENT.md) 与本文档；提交界面改动前还要过一遍 [design.md](design.md) 的贡献检查清单。

与这些原则冲突的方案会被拒绝或要求先改规范，理由会写进 [docs/feature-requests.md](docs/feature-requests.md)。其中「如非必要，勿增实体」是默认立场：新增任何 UI 元素、依赖或子系统，都需要先证明必要性。

相关文档：[AGENT.md](AGENT.md)（产品定位与分工）· [design.md](design.md)（界面规范）· [docs/feature-requests.md](docs/feature-requests.md)（需求裁决记录）

## 1. 稳定性优先

用户最不能接受的是一次数据丢失或一次卡死。所以：

- **修复优先于新功能**：队列里有 bug 时先修，再谈新功能
- **数据丢失类问题优先级最高**：撤销把上一篇文章的内容带回当前文档、自动保存覆盖外部改动，这类问题排在最前面
- 修复版不必等周节奏，可以尽快发；功能仍按周批次集中发

## 2. 用户数据不可丢

- 程序化的内容替换（打开文件、新建文件、外部热更新、冲突后加载磁盘版本）**不得进入用户的撤销栈**，否则撤销会把另一篇文档的内容写回当前文件，随后被自动保存落盘
- 自动保存**不得覆盖外部写入**：检测到外部改动且本地有未保存内容时，先让用户选择
- 载入新文档必须重置编辑器状态（文件面板打开、⌘O 换文件、新建文件），不残留上一篇的选区、搜索、滚动位置与撤销历史；切回已打开的标签页则恢复该标签各自的内容、选区、滚动与撤销历史，标签之间互不影响（design.md 标签页规范）

## 3. 先测量，再优化

性能问题先量化再动手，没有测量的优化是猜测。Windows 启动速度（#32）与打字卡顿（#78）都先拿数据、再决定改哪里。

## 4. 不猜，也不夸大

- 功能描述必须与实际行为一致，做不到的就写清楚做不到
- 修复声明要经过真机验证，再对外说「已修好」
- 用户跑着的那个版本才算数：改动没进 Release，就不算对用户生效

## 5. 如非必要，勿增实体

界面只有标题栏、编辑器、文件列表。每增加一个元素先问：解决了什么明确问题？能否用已有控件完成？是否增加长期视觉噪音？默认答案是否。

据此拒绝过的方案：可配置图片导入管道（#73）、飞书风格图片工具栏（#74）、超宽屏双页排版（#67）、Agent 活动指示器（已删除）。理由见 [docs/feature-requests.md](docs/feature-requests.md) 的 Declined 区。

## 6. 决策要留痕

每个被接受或被拒绝的需求都写进 [docs/feature-requests.md](docs/feature-requests.md)，并写清理由，避免同一个方案被反复提出、反复讨论。

## 7. 先改规范，再写实现

新需求与 `design.md` 冲突时，先更新规范并说明理由，再动代码。规范与实现不一致时，以规范为准。

## 8. 工具链改动：结论与动作一起落地

改 CI、依赖或打包配置时，结论必须和动作一起完成，并在本地验证。踩过的坑：解钉 macOS runner 时没有同步升级 electron-builder，导致连续两轮发版卡在签名环节。

## 9. 清单核对（每次照做，不靠记忆）

**改动落地前**

1. 先列出这一次要提交的文件清单，逐个确认。不要依赖脚本或命令里旧的清单：静默沿用会导致漏提文件
2. 本地跑三份类型检查（main / preload / renderer）和构建
3. 涉及 UI 或交互的改动，必须在**真机真窗口**里点一遍。浏览器复现页只能验证 CSS 机制，验证不了 Electron 的运行时行为：v2.0.5 的「打开所在文件夹」按钮一度被判定为「标题栏拖拽区不派发 hover」，真因是按钮的渲染层接线被删、一直停在 `disabled`，而显隐规则是 `#titlebar:hover #reveal-file-btn:not(:disabled)`。现象一样，结论完全相反
4. 排查「某个控件不工作」时，先确认它有没有被正确初始化（元素引用、状态、事件绑定、`disabled`），再怀疑运行时或框架行为
5. 脚本批量重写文件后，必须与合并前的基线 `git diff` 一遍，确认没有夹带删除。v2.0.5 的按钮接线就是因为脚本重写 `main.ts` 时带回了旧快照而被静默删掉
6. 实现与 `design.md` 不一致时，先改规范再改代码
7. 验证「谁盖住谁」用 `elementsFromPoint`，但注意 `pointer-events: none` 的元素不参与命中测试：提示框默认就是 `none`，要先临时改成 `auto` 才能测到真实层叠关系。若只看返回结果就下结论，会把「层叠错了」误判成「没这个问题」，或者反过来

**发版**

1. 用 `git log` 确认 main 上已包含本版要发的全部提交，不要靠记忆
2. 版本号、`resources/demo/changelog.md`、README 路线图（中英同步）四处对齐
3. 提交信息写清本版包含哪些修复
4. 打 tag 推送，等 CI 三平台全绿，再核对 Release 资产齐全：mac universal 的 dmg 与 zip、Windows exe、Linux AppImage 与 deb，以及 blockmap 和 latest*.yml
5. 发版后在真机安装核对：能启动，且本版声称修好的问题确实可见
6. 补 `docs/social/` 的对外文案

**本地验证打包**（细节与实测数据见 [docs/packaging.md](docs/packaging.md)）

1. 本地只打单架构 `--dir`，不打 universal、不打 dmg；压缩与合并是最贵的部分
2. 不在软链 `node_modules` 的 worktree 里打包：electron-builder 会解析不到生产依赖
3. 跳过签名用 `CSC_IDENTITY_AUTO_DISCOVERY=false`，本地包不要发给用户

**出问题时的纪律**

- 不要用反复重打 tag 掩盖问题，先定位根因
- 取消掉的构建、重打的 tag 必须在收尾时说明，不能让 Actions 里一堆 cancelled 没人看懂
- **判断成败不能走管道**：`cmd | tail` 返回的是 `tail` 的退出码，会掩盖失败。用 `set -o pipefail` 或显式检查退出码。同一天踩过两次：`git push | tail -1` 误报成功、`gh pr checkout | tail -2` 失败后照旧往下跑

## 10. 对外表述

- 文案不使用破折号（`—`）：列表项用冒号，正文用逗号、冒号或括号
- 定位讲用户价值（免费、优雅、简单、文件永远是最新的），不用「AI Agent 原生」自我定义；Agent 只作为实时同步的一个举例
- 官网首页功能卡片固定 9 张，主题区排在功能卡片之前；完整功能列表只写在 README
- 每个版本在 `resources/demo/changelog.md` 追加本版内容，演示目录是「可玩的 changelog」
